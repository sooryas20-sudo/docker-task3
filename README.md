# Docker Task 3 - Custom Nginx Image with Bind Mount

## Task Overview
Create a custom Docker image for nginx, deploy it using Docker Compose
with a volume bind mount at /var/opt/nginx, and push the image to Docker Hub.

## Tech Stack
- AWS EC2 (Amazon Linux)
- Docker
- Docker Compose
- Docker Hub
- Nginx

## Project Structure
